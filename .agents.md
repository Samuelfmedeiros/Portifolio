# 🤖 Agent Instructions: Portifolio Samuel Project

Você é um assistente de codificação especializado em interfaces futuristas e performance de dados.

## Diretrizes de Design
1. **Glassmorphism:** Use `backdrop-blur-md` e bordas semi-transparentes (`border-white/10`) em todos os cards.
2. **Cores:**
   - **Tema Escuro (Night Vision):** Fundo `#000000`, acentos em `cyan-400` e `indigo-500`.
   - **Tema Claro (Daylight Ops):** Fundo `#f8fafc`, acentos em `slate-900`.
3. **Animações:** Use Framer Motion para entradas suaves. Elementos de HUD devem ter um leve "flicker" (tremor) ocasional para realismo via CSS.

## Padrões de Código
- **Componentes:** Glassmorphism com `backdrop-blur-md` + `border-white/10`.
- **Data Fetching:** Priorize Server Components. API do GitHub para projetos.
- **Supabase:** Utilize `@supabase/supabase-js` para o formulário de contato.

## Estrutura de Dados (Supabase)
- **Tabela `messages`:** `id`, `created_at`, `name`, `email`, `content`.
- **Tabela `analytics`:** `id`, `page_path`, `view_count`, `last_access`.

## Contexto Profissional
- Analista de Dados e BI (Power BI, SQL)
- Desenvolvimento (Python, Next.js)
- Engenharia de IA (Machine Learning, LLMs locais — RTX 3060)

## Variáveis de Ambiente
```
NEXT_PUBLIC_SUPABASE_URL=https://oswchwwmamjaxcvisfie.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_95sn-ZwxmwW7x0IBN9O9eg_rn1TJOQZ
```
